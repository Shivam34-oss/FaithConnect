services:
  - type: web
    name: faithconnect-server
    runtime: node
    startCommand: node src/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
